# Nozawa Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# DATABASE
# ===========================================
# PostgreSQL connection string from Railway
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:PORT/railway

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# JWT secret for signing tokens (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# IMPORTANT: Use a strong random value in production!
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT token expiry (default: 24h)
JWT_EXPIRY=24h

# ===========================================
# EXTERNAL APIs
# ===========================================
# Google Places API key (for accommodation search)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_PLACES_API_KEY=your_google_api_key_here

# ===========================================
# CORS (Production)
# ===========================================
# Comma-separated list of allowed origins for CORS
# Example: https://nozawa.app,https://www.nozawa.app
ALLOWED_ORIGINS=https://nozawa.app,https://www.nozawa.app

# ===========================================
# RATE LIMITING (Optional - defaults provided)
# ===========================================
# Authentication rate limit (login attempts per window)
AUTH_RATE_LIMIT_MAX=5
AUTH_RATE_LIMIT_WINDOW_MS=900000

# API rate limit (requests per window)
API_RATE_LIMIT_MAX=100
API_RATE_LIMIT_WINDOW_MS=60000

# Admin rate limit (admin requests per window)
ADMIN_RATE_LIMIT_MAX=50
ADMIN_RATE_LIMIT_WINDOW_MS=300000

# ===========================================
# LOGGING & MONITORING (Optional)
# ===========================================
# Log level: error, warn, info, debug
LOG_LEVEL=info

# Enable request logging
ENABLE_REQUEST_LOGGING=true

# ===========================================
# FEATURE FLAGS (Optional)
# ===========================================
# Enable dual-write to both JSON and PostgreSQL
ENABLE_DUAL_WRITE=false

# Enable PostgreSQL for read operations
ENABLE_POSTGRES_READ=false

# ===========================================
# NOTES
# ===========================================
# - All values in this file are examples/placeholders
# - Copy to .env and replace with real values
# - .env is gitignored - safe to store secrets there
# - Railway environment: Set these in Railway dashboard
# - Never expose JWT_SECRET or DATABASE_URL publicly
